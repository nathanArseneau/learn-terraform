### 🔁 Day 58 — Recall 5: cold, then targeted

**Retrieve (30 minutes, nothing open)** — In `notes/recall-5.md`:

- [ ] The three situations where you write a `moved` block — renaming in your own configuration, moving resources into a module, and refactoring a module you publish — and who each one protects
- [ ] `tfe_outputs` versus `terraform_remote_state`: what each reads, and what each needs
- [ ] What `force_destroy` does, and when it is the wrong choice
- [ ] Every HCP Terraform run mode you triggered on Day 52
- [ ] From Part 1: the Day 26 runbook, symptom to command
- [ ] From Part 1: `count` versus `for_each`, and when `count` is still correct

**Targeted practice (60 minutes)** — Take the three slowest doc searches from Day 57's list and drill each one until you can find the answer in under a minute.
