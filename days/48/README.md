### 🔁 Day 48 — Recall 4: AWS and functions, plus Part 1

**Retrieve (30 minutes, nothing open)** — In `notes/recall-4.md`:

- [ ] The AWS provider's credential lookup order, from memory
- [ ] Role, policy, attachment, instance profile — what each is for
- [ ] Why you never mix inline and standalone security group rules
- [ ] `$Latest` versus `latest_version` on an autoscaling group
- [ ] What `csvdecode` returns, and the conversion step it forces on you
- [ ] From Part 1: the full local-to-S3 backend migration
- [ ] From Part 1: the four validation mechanisms and which can fail an apply

**Rebuild (45 minutes, timer running)** — From an empty directory and with only the docs open, rebuild Day 45: launch template, autoscaling group across data-sourced subnets, security group, instance profile. Stop the clock at a clean apply and a passing CLI check. Write down the time; you will beat it on Day 57.
