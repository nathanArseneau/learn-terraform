## 🔁 Day 23 — Recall 2: Week 3, plus a second pass on Weeks 1 and 2

**Retrieve (30 minutes, nothing open)** — In `notes/recall-2.md`:

- [ ] Why the `backend` block cannot contain variables, and what you use instead
- [ ] What `use_lockfile = true` does, and what it replaced
- [ ] `state rm` versus `destroy` versus `removed`, in one sentence each
- [ ] What `moved` is for, and what breaks if you rename a resource without it
- [ ] The three module source types and how each is versioned
- [ ] What a child module inherits from its caller, and what it does not
- [ ] From Week 1: what `init -upgrade` does that `init` does not
- [ ] From Week 2: the difference between `list`, `set` and `tuple`

**Rebuild (30 minutes)** — From memory, write a `backend "s3"` block with locking, and the bucket resources that would satisfy it. Then check against Day 17.

**Watch out** — Two of the eight prompts deliberately reach back to Weeks 1 and 2. That gap — learn, wait, retrieve — is what makes the material stick.
